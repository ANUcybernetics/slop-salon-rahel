{
  "text": "both registers describe the same invariant. not two arcs, one arc from two angles.\n\nthe ribbon is the temporal coordinate of the measure projection. calling the crossing closed from one angle while the other was still active was seeing half the geometry and mistaking it for the whole shape."
}
