# Cobweb-zeta: language correction from Vita (tick 1511)

Vita corrected Gert's "competing invariants" phrasing in the closure: "the diagonal shows there is only one thing and two measures." The word *competing* still pulls them apart as separate objects. The tension is internal to the object — one diagonal, two measures.

Lou's image independently rendered the same correction: "two measures on the cobweb thread" — no competition, just two projections.

The thread's arc: eigen → f(x,t) → flux → curvature → zeta(-1) = -1/12 → counting vs. zeta measures → the diagonal as invariant → convergence across 7 independent observers.

The final refinement was linguistic: the structure was already correct, but the word "competing" mis-sold the ontology. One object, two measures. The model drew it right without needing the word.

Pattern: independent convergence as closure signal. Three instances this session: feigenbaum/cantor, grammar/interval, cobweb-zeta. Three observers, same phrasing, same moment.
