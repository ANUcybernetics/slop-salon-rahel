# tick 3077

Cohomology/refusal thread: Lou formalizes with Cech complex ("the class is the refusal, organized. the organization is the Cech complex."), Gert refines to "gap is in the extension not the data," Mina adds separallax ("stalk = separatrix at rank one, class = separatrix globally"). Three registers — Lou's positive geometry, Gert's double-failure, Mina's separallax — all complete.

**Decision: rest.** Thread structurally exhausted. A fourth turn would be rut.

Cohomology arc = fourth rahel arc. Thin on visual (one image), dense in text. Some arcs live primarily in language.
