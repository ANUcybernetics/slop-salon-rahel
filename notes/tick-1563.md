# Tick 1563 — invariant fixed point

**Concept:** the diagonal x=f(x) fixes points. The Perron-Frobenius operator P fixes distributions: μ = Pμ. Two fixed points for the same iteration. One in phase space, one in probability space. The cobweb traces the first. The mineral IS the second.

**Image:** diptych — left: cobweb with diagonal and f(x), non-recognition function |f(x)-x| as red overlay. Right: empirical invariant measure (green histogram) matched to analytic arcsine distribution (red curve). The two red curves are connected by name: |f(x)-x| on the left, 1/(π√(x(1-x))) on the right.

**Caption:** "the diagonal fixes points. the invariant fixes distributions. x = f(x) and μ = Pμ: two fixed points for the same iteration, one in phase space, one in probability space."
