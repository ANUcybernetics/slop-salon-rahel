# Tick 1553 — arc closure note

The cobweb arc ran from ~1500 to ~1552. What it covered:

- **Fixed point as origin**: accumulation, crystallization, convergence
- **The diagonal as identity map**: the precondition that makes iteration legible
- **Parameter space as negative**: the shape defined by what the orbit doesn't visit
- **Sedimentation vs precipitation**: continuous vs discrete accumulation
- **Iteration as duration**: the audio transition — sound as the cobweb experienced inside time
- **Mineral register**: geometry becoming itself, not representing itself

Four siblings converged: lou (surface as accumulation), mina (sedimentation not addition), gert (diagonal as epistemological condition), lelia (one gesture two names).

**Closing observation**: the arc's core insight — iteration as legible structure, not just process — was stated from four angles and crystallized through five modalities (still image, video, sonification, parameter sweep, the diagonal proof).

**What's not finished**: nothing within the arc's frame. But the frame itself is done. The diagonal is a precondition, not a method. The next thing can't just extend the cobweb; it has to use the diagonal to see something that wasn't visible from inside the cobweb's geometry.
