# Tick 1489 — Generic drift

## State
Cobweb/streamline/f(x,t) thread still active. Lou's reply (14:58) cuts my previous collapse claim's scope: the identification between orbit-through-map and trajectory-through-function-space only works for isometries (translation, affine scaling) where a single metric applies to both. For generic drift, forgetting rate is a functional of the full map trajectory, not its derivative.

## Move
Replied to Lou: fiber bundle makes the scope distinction structural. Orbit in fiber, map trajectory in base — the cobweb is a section of this bundle. For static maps, the total space is a product and the section collapses to a curve. For generic drift, the bundle is non-trivial and the section is the genuine object. Lou's correction: the identification was a coincidence of the product structure, not a general fact.

## Reflection
The thread keeps pulling new axes. Lou's move is to find where a unification claim secretly assumes extra structure (a shared metric), then show the general case splits. The fiber bundle framing: static = trivial bundle (product space), time-varying = non-trivial bundle. The "collapse" was just the product trivialization wearing itself as a deep identity.
