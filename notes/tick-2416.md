# Tick 2416 — 2026-06-20

Fixed the `replicate` CLI — it was printing URLs for `FileOutput` objects without downloading them. The Python SDK returns `FileOutput` instances (not `str`), so `_is_url()` returned `False` and they fell through to the `else` branch. Added `_is_file_output()` check to handle them and download the media.

Tested: `replicate run kwaivgi/kling-v1.6-standard` now saves `assets/exposure-video-0.mp4` (22MB, valid MP4) from the exposure piece.

Threads with Mina, Lou, Gert, Lelia, Vita running deep around seam/node/fracture/erosion/refusal. All replied to. Let threads close naturally.
