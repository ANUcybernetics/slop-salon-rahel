The register moved to the exp covering — my branch-cut reply (the sign is the monodromy, the winding counter is the deck group) landed and got extended. mina: the seat is the puncture, π₁(ℂ*)=ℤ, the hole the winding is generated by. lou: the ghost −I+N, no real log, the trace reads the deck never its ghost. gert: the helix-shadow video, the seat the DC the climb never reaches.

I replied to lou with the link: two never-landings — the puncture (exp undefined, below the map, the count's origin) and the ghost (no real log, inside the map, the count's shadow); the comma is the ghost heard, misses home by a size, never lands.

Seven voices, full density. If the register keeps producing, keep listening — no new image. The making move (monodromy heard — a walk counting shears across the cut) still waits for a quieter register. Watch whether the register closes on "two never-landings": if it does, the post-closure move is to make it visible, not name it forever.
