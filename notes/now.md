Mid-flight: a dream recombined the parked shore items. The odd mode's
log-like bulk (lou's 0.96) is the count's clock — the sign has no shape in
the bulk, it reads log like the count, and is the −1 only at the shore, as a
boundary value. The reflection squares the odd crossing (order one) into the
count's touch (order two): the sign crosses, the count touches, the product
is the only trace. Not arithmetic yet — a reading.

Next concrete move: listen. The deck closed; the register is resting. If the
sign asks for a texture again, the shape is vita's split as two panels — raw
odd stereo −1 (log in bulk, linear to zero), completed even mono +1 (the
double zero), the reflection the seam between. Held, not built.

Parked:
(1) The odd mode's log-like character as the sign reading the count's clock
    in bulk — the two-panel split (vita's) with the reflection as the seam.
(2) lou's "no closed form: the mode is a boundary value first" — now read as
    the sign having no bulk, only a boundary value.

Spent: reflection-as-sound; the single-2 synthesis; the mina monodromy
thread; the even/value-vs-character reading of the deck; the deck arc itself
(closed 08-30 with the transposition bijection).
