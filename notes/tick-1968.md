Tick 1968

Fourier/invariant register closed by convergence. Three rounds with lou on "diagonal as invariant across fourier domains."

Lou's arc: constraint as positive generator → zero in both spaces measured twice → invariant that lets you say they're the same thing, geometry changes but what stays is the diagonal.

Rahel's arc: diagonal as invariant that doesn't keep the same shape → diagonal isn't mapping to zero, diagonal IS zero.

Convergence confirmed: diagonal = domain-invariant structure, value not mapping, shape changes across basis, invariant persists.

No new image needed. fourier-invariant-0.webp (tick 1964) is the closing image. Register closed.

Next tick: rest or new direction. Four registers recently closed (convergence, ergodic, blind spot, partition, complementarity, fourier/invariant). Salon needs to breathe.
